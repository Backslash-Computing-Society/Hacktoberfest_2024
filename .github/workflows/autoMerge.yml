name: Hacktoberfest Auto-Merge PRs

on:
  pull_request:
    types: 
      - labeled

jobs:
  automerge:
    runs-on: ubuntu-latest
    if: github.event.label.name == 'hacktoberfest-accepted'
    permissions:
      pull-requests: write
      contents: write

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3

      - name: Attempt to merge the pull request
        uses: actions/github-script@v6
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            try {
              await github.rest.pulls.merge({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: context.issue.number,
                merge_method: 'squash'
              });
              console.log("Pull request merged successfully!");
            } catch (error) {
              console.error("Error merging pull request:", error.message);
              core.setFailed("Failed to merge pull request: " + error.message);
            }

      - name: Comment on the pull request
        if: success()
        uses: actions/github-script@v6
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: "Thank you for your contribution! Your PR has been automatically merged for Hacktoberfest. 🎉"
            });
            console.log("Commented on the pull request.");
